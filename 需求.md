# Clawith × Agency Orchestrator 整合需求规格

| 项 | 内容 |
|----|------|
| 版本 | v3.0（基于 v2.5 重排 + 与现网治理入口对齐） |
| 日期 | 2026-07-27 |
| 分支基线 | `feat/retry-run-loop` |
| 关联设计 | `docs/superpowers/specs/2026-07-27-requirement-entry-governance-groups-design.md` |
| 关联计划 | `docs/superpowers/plans/2026-07-27-ao-orchestrator-integration.md` |
| 读者 | 产品 / 研发 / 测试 / 运维 / 交付 |

---

## 0. 文档导读

### 0.1 一句话目标

把 Agency Orchestrator（AO）的 YAML/DAG 编排能力嵌入 Clawith，形成：

**HR 群提需求 → 确认方案建执行群 → 群主纯调度 → 多角色执行（执行 N 个） → 独立质控（1~2 个） → 专职交付（1 个） → 自主进化/技能自学 → 群文件夹沉淀 → 度量大盘。**

### 0.2 与现网已确认产品决策的对齐（重要）

| 主题 | 本需求采用结论 |
|------|----------------|
| 需求入口 | **HR 评审群新建会话 = 提需求**（不再以「创建项目」为主入口；底层可保留 workflow 实体） |
| 方案确认 | 群内 ≥3 方案短卡 + 弹窗全文；确认后创建 Agent/执行群，并跑原初始化对话 |
| 常驻群 | 租户注册/启动即有 **HR 群**、**股东群**（CEO/CTO/COO/CFO + Board Secretary 为股东群主） |
| 决策升级 | 执行群 ← 决策群 ← 股东群；股东不定论不直接改任务；人类确认统一 @董事会秘书 |
| AO 位置 | AO 作为**编排执行引擎**，由执行群内「项目调度官」调用，不另开一套入口 |

> 旧稿「项目入口唯一 / 项目管理模块新建需求」改为：**产品入口在 HR 群；项目管理/执行列表作为「执行中的需求」视图。**

### 0.3 范围速览

**做：** 四权分立角色、AO 集成、质控/交付闭环、资产沉淀、进化与技能自学、度量大盘、30 模板。  
**不做：** AO 底层 DAG 重构、Clawith Runtime 核心重写、第三方 IM 深适配、可视化拖拽编排器、本地大模型推理。

---

## 1. 背景与目标

### 1.1 背景

Clawith 已有 Agent 身份、组织、群聊、审批、模型网关、项目工作流与 `retry-run-loop` 运行循环。AO 擅长 YAML 零代码编排、角色库与断点续跑。当前问题：入口分散、调度/质控/交付职责混杂、能力固化、资产难沉淀、效能难量化。

### 1.2 业务目标

1. 入口统一到 HR 需求会话 → 执行群工作流  
2. 四权分立：调度 / 执行 / 质控 / 交付  
3. 质量可控：质量评审官 + 规则引擎；交付验收 ≥90 分准入  
4. 自主进化 + 技能自学  
5. 全量资产进群文件夹  
6. 多级度量大盘（效率/成本/质量/进化/技能）

### 1.3 技术目标

1. AO 无侵入封装为后端服务 + Agent 工具  
2. LLM 全走 Clawith 模型网关  
3. 复用 `retry-run-loop` 与现有群/任务/决策链路  
4. 独立质检引擎、进化引擎、技能引擎（可分期）  
5. 群文件夹映射 AO `--output`  
6. 四级～五级度量统计

### 1.4 四权 + 多角色

> **"四"指的是四类权力位（调度/执行/质控/交付），"多"指每类下有多个 Agent 角色。**
> 执行群不是 4 个人，而是 **调度 1 + 执行 N + 质控 1~2 + 交付 1 + 干系人** 的小队；N 由方案与 DAG 决定。

#### 1.4.1 四权与典型角色清单

| 权力位 | 必备角色 | 可选角色 |
|--------|----------|----------|
| 调度 | 项目调度官（群主） | 子调度（按区域/阶段拆分大项目时） |
| 执行 | 由 HR 方案确认：每个 AO 角色 → 一个 Clawith Agent | 干系人 / 信息官 / 数据采集员 / 法务顾问 … |
| 质控 | 质量评审官（主） | 合规复核官 / 安全评审官 |
| 交付 | 交付协调官 | 验收记录员 / 客户对接官 |

#### 1.4.2 职责与边界

| 类型 | 职责 | 边界 |
|------|------|------|
| 调度 | DAG、分发、推进、异常协调、审批触发 | 不执行、不定质量、不直接交付 |
| 执行（多角色） | 各业务 Agent 按角色定义出交付物；可触发技能自学 | 不干预质控判定；不在自身以外范围越权 |
| 质控 | 质检、评分、整改意见 | 不改原始业务稿、不调度 |
| 交付 | 整理交付、对接真人交付经理 | 不定质量、不改业务稿 |

#### 1.4.3 角色来源与数量约定

- **HR 出方案时按业务**给出执行角色清单（≥1，可任意多），不再硬约束 4 人。
- 调度/质控/交付每个权力位 **至少 1 个 Agent**；质控可叠加合规/安全角色。
- 群成员上限默认 30；超过走拆群/子执行群。
- 股东群 / 决策群仍按治理设计处理"定不下来"的升级拍板，不与四权角色混岗。

#### 1.4.4 与治理侧的关系

- HR 群 = 4 常驻（Recruiter / Org Designer / Strategist / HR Secretary），负责出方案卡
- 股东群 = CEO/CTO/COO/CFO + Board Secretary（群主）
- 决策群 = 自治理池选人；与上述四权角色解耦

### 1.5 术语

| 术语 | 定义 |
|------|------|
| AO | Agency Orchestrator |
| 工作流实例 | 一次执行生命周期；对应执行群 + 资产目录 |
| DAG | 步骤依赖图 |
| 断点续跑 | 从指定步骤重跑并复用前置产物 |
| 技能 | 工具 / 流程 / 知识包统称 |

---

## 2. 架构

### 2.1 分层（摘要）

```
前端：需求与执行 / 群聊 / 模板 / 资产 / 度量 / 角色·技能中心
后端：项目·workflow / AO 服务 / 质检 / 进化 / 技能 / LLM 网关 / 度量
运行时：调度官 · 多执行角色 · 质控官 · 交付官（+ 治理侧 HR/决策/股东）
基建：PG / Redis / 群文件夹
```

### 2.2 设计原则

1. 四权 + 多角色（执行位下可任意角色数）  
2. 能力绑定角色（非绑死在群主）  
3. 自主成长可回滚  
4. 技能安全优先（沙箱 + 分级审核）  
5. 资产全量沉淀  
6. 产品入口在 HR；执行在执行群  
7. 优先复用 Clawith 原生能力  
8. AO 无侵入集成  

### 2.3 模块职责

| 模块 | 职责 |
|------|------|
| HR 评审群 | 需求会话、多方案、确认建执行群 |
| 执行群 + 调度官 | DAG 调度、推进、异常与审批触发 |
| 执行角色池 | HR 方案中每个业务角色 → 一个 Agent；可技能自学 |
| 质控 / 交付官 | 质检与验收对接 |
| AO 服务 | YAML/DAG/断点续跑 |
| 质检 / 进化 / 技能引擎 | 规则、自优化、技能市场与沙箱 |
| 群文件夹 | 结构化资产 |
| 度量引擎 | 聚合与看板 |

---

## 3. 端到端流程

### 3.1 总览

```
HR 群新建会话（提需求）
  → HR 出 ≥3 方案（含角色 soul/工具）→ 用户确认
  → 创建执行群 + Agent +（按需）决策群 → 初始化对话
  → AO Compose：YAML + 多角色（调度 1 + 执行 N + 质控 1~2 + 交付 1）+ DAG
  → 调度官循环：分发 → 多执行角色（可技能自学）→ 质控 → 通过则推进 / 不通过则重试
  → 全完成 → 交付协调官申请验收 → 真人交付经理评分
  → ≥90 通过归档 + 成长；＜90 整改再跑
  → 回写执行视图 + 度量
```

决策升级（并行治理能力）：决策群定不了 → 股东群（@董事会秘书）→ 回决策群 → 再下发执行群。

### 3.2 阶段摘要

| 阶段 | 要点 |
|------|------|
| 1 需求 | HR 会话；参数：模板/智能生成、模型、重试、质检阈值、交付经理、技能权限 |
| 2 初始化 | Compose YAML；建执行群；调度官群主；拉 **N 个执行角色** + 质控 + 交付 + 干系人；目录与公告 |
| 3 执行+质控 | DAG 分发；技能自学；步后质检；审批/决策挂起；失败重试 |
| 4 人机交互 | 审批卡、决策卡、高危技能审核、质检异常人工复核 |
| 5 交付验收 | 清单+范围核对；双维度评分；整改闭环（≤3 次预警） |
| 6 成长 | 进化（记忆/Prompt/回归/版本）+ 技能沉淀入库 |
| 7 收尾 | 正式包、索引、度量同步、支持迭代续跑 |

---

## 4. 功能需求（按模块）

### 4.1 角色与工具（摘要）

**调度官工具：** `ao_parse_workflow` / `ao_get_execution_plan` / `ao_resume_from_step` / `dispatch_task_to_role` / `init_workflow_dir` / `update_workflow_status` / `send_channel_message` / `update_project_status` / `trigger_approval_node` / `audit_skill_application`

**质量评审官：** `quality_check_step` / `quality_check_full` / `verify_rectification` / `get_quality_rules` / `generate_quality_report` / `write_quality_asset` / `submit_feedback_to_role` / `learn_quality_skill`

**交付协调官：** `compile_delivery_package` / `check_requirement_coverage` / `submit_approval_request` / `parse_rectification_comments` / `generate_delivery_report` / `write_delivery_asset` / `update_approval_status` / `learn_delivery_skill`

**执行角色（多角色，按方案）：** AO 角色库映射至 Clawith Agent；每个角色一个 Agent；提交输出接受质检；可技能自学与进化。
  - N 由 HR 方案决定；同一群内可并存多个执行 Agent（PM、前端、后端、数据、法务…）
  - 角色间协作通过群消息 + 调度官分发；执行角色不在自身职责外越权

### 4.2 自主进化

反馈采集 → 长期记忆 → Prompt/策略小步优化 → 回归验证 → 版本发布/回滚 → 效果度量（质量/效率/成本/稳定性）。

### 4.3 技能自学

识别缺口 → 检索/生成 → 沙箱测试 → 风险分级（低/中自动，高危人审）→ 上线 → 效果反馈 → 企业市场沉淀。  
要求：需求识别准确率 ≥90%；调用可审计；可一键禁用。

### 4.4 产品与前端

| 面 | 内容 |
|----|------|
| 需求与执行 | HR 入口、执行中列表、决策群/执行群入口（已落地基线可扩展） |
| 执行详情 | 进度、步骤时间线、验收、角色/技能、资产、迭代树 |
| 角色成长中心 | 版本、进化轨迹、技能库、策略配置 |
| 技能后台 | 市场、审核队列、统计、安全审计 |
| 模板库 | 30 官方 + 企业自定义 |
| 交付验收中心 | 待验/在验/已验、双维评分 |
| 度量大盘 | 组织/项目/模板/角色/技能；ROI；预警 |

### 4.5 群聊话术规范

初始化公告、任务分发、技能学习/审核、质检通过/不通过、验收申请/驳回、进度汇总——均按角色发言，调度官不越权发质量/交付结论。

### 4.6 AO 编排

- 集成：Node 子进程 CLI + FastAPI 封装；整流程/单步；状态流式回传  
- 能力：并行 DAG、变量传递、重试、断点续跑、条件分支、人工审批、角色版本与技能权限绑定  
- 模板：30 官方，默认可带四类角色配置  

### 4.7 群文件夹目录（规范）

```
{工作流实例}/
  00-工作流定义/  01-步骤输出/  02-过程记录/
  03-质量管控/    04-交付验收/  05-技能档案/
  06-最终交付/    07-历史迭代/
```

AO `--output` 指向实例根；角色按职责写目录；权限对齐群成员。

### 4.8 模型网关

Clawith 模型全量同步 AO；统一 Provider；流式；配额与审计；步骤级模型与质检驱动升配。

### 4.9 审批与分支

兼容 AO `approval`；扩展审批人匹配与超时策略；`human_choice` 群内选项；记录可回溯。

### 4.10 智能编排增强（可后置）

模板 Top3 匹配、执行中动态调参、异常自主决策、cron/Webhook 触发。

### 4.11 质检与验收

规则引擎（文档/代码/数据）→ 步检/全检 → 百分制 → 交付经理范围 40% + 质量 60%，≥90 通过。

### 4.12 度量

五级 × 五维指标；看板下钻与导出；ROI；质量/技能/进化预警。

---

## 5. 非功能需求

| 类 | 关键指标 |
|----|----------|
| 性能 | 建群 ≤5s；状态同步 ≤1s；步检 ≤10s；20 并行；大盘首页 ≤2s；技能学习 ≤2min；单轮进化 ≤30min |
| 安全 | 租户隔离；密钥仅网关；审计；沙箱；高危人审；Prompt/进化数据加密 |
| 兼容 | 不破坏 `retry-run-loop`；兼容现有 Agent/RBAC/审批；AO YAML；PG/SQLite |
| 可维护 | AO 适配层独立；Alembic；规则热更新；引擎可启停 |

---

## 6. 数据模型（核心表清单）

> 详细字段见附录 A；实现时按阶段增量迁移。

| 表 | 用途 |
|----|------|
| `project_workflow_runs` | 工作流实例 |
| `workflow_run_steps` | 步骤与质检分 |
| `workflow_approvals` | 步骤审批 |
| `agent_roles` / `agent_role_versions` / `agent_evolution_records` | 角色与进化 |
| `agent_skills` / `agent_role_skill_bindings` / `skill_learning_records` | 技能 |
| `workflow_templates` | 模板 |
| `workflow_quality_rules` / `workflow_quality_records` | 质检 |
| `workflow_delivery_approvals` | 交付验收 |
| `workflow_metrics_daily` | 日度量 |

兼容：任务/频道文件增加 `workflow_run_id`；Agent 扩展版本关联；全表 `tenant_id`。

---

## 7. 实施计划（摘要）

> **完整可执行计划见：**  
> [`docs/superpowers/plans/2026-07-27-ao-orchestrator-integration.md`](docs/superpowers/plans/2026-07-27-ao-orchestrator-integration.md)

| Phase | 名称 | 工期 | 里程碑 |
|-------|------|------|--------|
| P0 | 基线对齐 | 1 周 | 与 HR/股东治理入口、retry-run-loop 对齐；表结构草案 |
| P1 | AO + 多角色骨架 | 3 周 | HR 确认 → 执行群 → 多角色（调度+执行N+质控+交付+干系人）；AO parse/run API |
| P2 | 调度+质控+资产 | 3 周 | 分发→执行→质检→归档闭环 |
| P3 | 交付验收 | 2 周 | 交付官 + ≥90 验收闭环 |
| P4 | 自主进化 | 2 周 | 进化引擎 + 版本回滚 |
| P5 | 技能自学 | 3 周 | 市场 + 沙箱 + 高危审核 |
| P6 | 智能编排+度量 | 3 周 | 匹配优化 + 五级大盘 |
| P7 | 硬化上线 | 1 周 | 性能/安全/灰度 |

**总工期建议：18 周。**  
**前置已完成（不计入工期）：** HR/股东常驻群、方案卡确认建群、决策升级链、输入压缩等（`feat/retry-run-loop` 治理改造）。

### 资源建议

前端 2 · 后端 3 · Agent/全栈 2 · 测试 1 · 产品 1

---

## 8. 验收标准（摘要）

1. HR 提需求 → 确认 → 执行群**多角色**（调度 1 + 执行 N + 质控 1~2 + 交付 1 + 干系人）+ AO 可跑通
2. 调度不越权；质控/交付边界清晰；执行 N 可按方案扩展  
3. 步检/全检与 ≥90 交付闭环  
4. 进化可回滚；技能沙箱+高危人审  
5. 群文件夹 8 类资产齐全  
6. 模型走网关、配额可审计  
7. 30 官方模板可启动  
8. 性能/安全/兼容满足第 5 章  

本项目自身交付亦按百分制 ≥90 验收（功能 30 / 架构 20 / 稳定 20 / 交付质量 15 / 性能安全 15）。

---

## 9. 风险与应对（摘要）

| 风险 | 等级 | 应对 |
|------|------|------|
| AO 上游不兼容 | 中 | 适配层锁定版本 |
| 质检误判 | 高 | 规则+LLM+人工兜底 |
| 进化退化 | 高 | 小步+强制回归+回滚 |
| 技能安全 | 高 | 分级+沙箱+人审+审计 |
| 调度中断丢状态 | 高 | 状态持久化+重启续跑 |
| 群消息轰炸 | 中 | 折叠+重要才 @ |
| 配额打爆 | 中 | 预检+单次上限 |

---

## 10. 后续迭代（非本版）

可视化 DAG 编辑器、飞书/钉钉/企微、工作流/技能市场交易、跨组织协作、多模态、成本智能路由、联邦技能市场。

---

## 附录 A：核心表字段（精简）

### A.1 `project_workflow_runs`

`id, tenant_id, project_id?, requirement_id?, workflow_name, yaml_content, template_id?, group_id, scheduler_agent_id, quality_agent_id, delivery_agent_id, delivery_manager_id, executor_agent_ids (JSON 数组), stakeholder_agent_ids (JSON 数组), member_count, created_by, status, approval_status?, final_score?, model, concurrency?, max_retries?, quality_threshold?, skill_permission_level, allow_custom_skill, total_tokens?, asset_dir_path, latest_version, started_at?, completed_at?, parent_run_id?, created_at, updated_at`

### A.2 `workflow_run_steps`

`id, run_id, step_id, step_order, role, agent_id?, agent_version, task, used_skill_ids?, status, quality_score?, output_file?, input_tokens?, output_tokens?, retry_count, depends_on?, started_at?, completed_at?`

### A.3 其余表

见原 v2.5 §6.1.3–6.1.14（审批、角色版本、进化、技能绑定、学习记录、模板、质检规则/记录、交付验收、日度量）——实现时按 Phase 拆分迁移，不一次性上全量。

---

## 附录 B：群聊文案模板

保留 v2.5 §4.5 中的初始化 / 分发 / 技能 / 质检 / 验收话术作为交互规范；实现时收入 i18n 与调度官 system prompt。

---

## 修订记录

| 版本 | 说明 |
|------|------|
| v2.5 | 原稿正式版 |
| v3.0 | 重排结构；对齐 HR 入口与股东治理；抽出独立实施计划文档；附录化超长表结构 |
